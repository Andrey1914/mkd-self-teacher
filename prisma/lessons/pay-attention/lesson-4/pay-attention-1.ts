import { PayAttentionBlockProps } from "@/types";

export const payAttention1: PayAttentionBlockProps = {
  sections: [
    {
      type: "pay-attention-1",
      slug: "lesson-4",
      title: "ОБРАТИТЕ ВНИМАНИЕ!",
      content: {
        text: `Глаголы <span>ве*ли</span> и <span>збо*рува</span> переводятся как «говорить». <span>Збо*рува</span> используется для обозначения
        процесса говорения в некторый момент времени (<span>се*га</span> <span>збо*рува</span> «сейчас говорит»), во фразах типа
        <span>збо*рува</span> <span>маке*донски</span> «говорит по-македонски». <span>Ве*ли</span> обычно вводит прямую и косвенную речь:
        <span>ве*ли</span> <span>де*ка...</span> «говорит, что...»
        `.trim(),
      },
    },
  ],
};
